apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: cnpe-packaging

resources:
- ../../base

nameSuffix: -prod

replicas:
- name: my-app
  count: 3

patches:
- path: deployment-patch.yaml
