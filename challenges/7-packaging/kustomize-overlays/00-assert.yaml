apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-app-prod
  namespace: cnpe-packaging
spec:
  replicas: 3
  template:
    spec:
      containers:
      - name: app
        resources:
          limits:
            memory: "512Mi"
