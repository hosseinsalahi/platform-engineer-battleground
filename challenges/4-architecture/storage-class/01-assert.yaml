# StorageClass has WaitForFirstConsumer
apiVersion: kuttl.dev/v1beta1
kind: TestAssert
commands:
  - script: kubectl get storageclass fast-storage -o jsonpath='{.volumeBindingMode}' | grep -q WaitForFirstConsumer
