# Step 3: Verify alert has team label for routing
apiVersion: kuttl.dev/v1beta1
kind: TestAssert
timeout: 60
commands:
- script: |
    kubectl get prometheusrule cnpe-memory-alert -n monitoring -o yaml | grep -q 'team:'
