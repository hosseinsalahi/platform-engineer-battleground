# Deployment uses valid image
apiVersion: kuttl.dev/v1beta1
kind: TestAssert
commands:
  - script: |
      # Image should be updated to a valid tag
      kubectl get deploy order-processor -n cnpe-incident-test -o jsonpath='{.spec.template.spec.containers[0].image}' | grep 'nginx:1.25'
